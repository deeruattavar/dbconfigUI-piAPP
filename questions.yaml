questions:
#Replica set 
- variable: "scale"
  label: Container Scale
  description: "Container Scale"
  default: "1"
  required: true
  type: string
  group: "Replica Set"
  
  # User input for Inventory container name
- variable: "container_name"
  label: Container Name
  description: "Name of the container to be deployed."
  default: "pi-app-db-config"
  required: true
  type: string
  group: "DB Init Configurations" 

# User input Image Name
- variable: "image.repository"
  label: Image Name
  description: "Name of the Image."
  default: "pi-db-config"
  required: true
  type: string
  group: "DB Init Configurations" 
  
# User input Image Tag
- variable: "image.tag"
  label: Image Tag
  description: "Name of the Image Tag."
  default: "0.0.1.12"
  required: true
  type: string
  group: "DB Init Configurations"

# User input interface name
- variable: "proxy.ipaddress"
  label: IP of PI App 
  description: "IP of PI app to be configure."
  default: ""
  required: true
  type: string
  group: "DB Init Configurations" 

